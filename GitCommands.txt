Git init
git status
git add <filename> -  Add a file to the staging environment
git commit -m "Your message about the commit"
git checkout -b <branch name>
git branch
git remote add origin <repo name from github
git push -u origin master>
git push origin <my-new-branch>
git log